# Glossary

- `LM Studio`: local model server with OpenAI-compatible REST endpoint.
- `OpenClaw`: agent runtime container.
- `workspace`: mounted runtime I/O directory (`/home/openclaw/.openclaw/workspace` in container).
- `savedPath`: screenshot target path returned by browser tool.
- `MEDIA:` directive: inline marker used by message pipeline for media delivery.
